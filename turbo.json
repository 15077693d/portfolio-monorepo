{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build:web": {
      "inputs": [".env"]
    },
    "build:api": {
      "inputs": [".env"]
    },
    "dev": {},
    "start": {},
    "build": { "inputs": [".env"] },
    "start:web": {},
    "start:api": {}
  }
}
